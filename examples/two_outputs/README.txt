This model is like the "missing_commodity" example, except that it includes transport related supply
chains, and one processes that produces two SED outputs (OILREF), and one process that consumes two
SED inputs (THYBCR).
