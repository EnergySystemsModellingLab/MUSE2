[project]
name = "muse2-docs"
version = "0.1.0"
description = "Scripts to build the MUSE2 documentation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.6",
    "pyyaml>=6.0.3",
    "table2md>=1.1.0"
]

[tool.uv]
package = false
